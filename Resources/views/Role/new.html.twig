{% extends 'InstinctUserBundle::layout.html.twig' %}


{% block container %}
<h1>Role creation</h1>

<form action="{{ path('instinct_user_admin_role_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('instinct_user_admin_role_index') }}">
            Back to the list
        </a>
    </li>
</ul>

{% endblock %}